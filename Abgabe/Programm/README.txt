README:

1. place a file with the name "peronendaten.txt" in the folder, where "read_text_file.py" is stored.
2. run "read_text_file.py" with a paython interpreter
3. see results in: "personendaten.json"